[workspace]

members = [
    "analysis",
    # "idxpkg",
    "parser",
    "replayshark",
    # "replayserver",
]

[workspace.dependencies]
wowsunpack = { version = "0.7", default-features = false, features = ["json"] }