[workspace]

members = [
    "analysis",
    # "idxpkg",
    "parser",
    "replayshark",
    # "replayserver",
]

[workspace.dependencies]
wowsunpack = { version = "0.6", default-features = false, features = ["json"] }